package damdariar.gui;

import org.hibernate.Session;
import org.hibernate.Transaction;

import damdariar.hibernate.HibernateUtil;

public class startUp {
	
   public static void ubdateStatusCow(){
	  /* String str = "declare bool boolean; bool1 boolean; begin bool:=true;  bool1:=updateStatusCow(bool); end;";
	   try {
		   	  Session sess = HibernateUtil.getSession();
			  Transaction trx = sess.beginTransaction();
			  sess.createQuery(str).executeUpdate();
			  trx.commit();
			  HibernateUtil.closeSession();
			 
		} catch (Exception e) {
			System.out.println(e);
		}*/
   }
}
